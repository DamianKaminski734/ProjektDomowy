<Window x:Class="ProjektDomowy.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Biblioteka Domowa" Height="450" Width="800">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Biblioteka Domowa" FontSize="22" FontWeight="Bold" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10" VerticalAlignment="Top" HorizontalAlignment="Left">
            <StackPanel Margin="0,0,10,0">
                <TextBlock Text="Tytuł:" Margin="0,0,0,5"/>
                <TextBox Width="150" Text="{Binding NewTitle}" />
            </StackPanel>

            <StackPanel Margin="0,0,10,0">
                <TextBlock Text="Autor:" Margin="0,0,0,5"/>
                <TextBox Width="150" Text="{Binding NewAuthor}" />
            </StackPanel>

            <StackPanel Margin="0,0,10,0">
                <TextBlock Text="Rok wydania:" Margin="0,0,0,5"/>
                <TextBox Width="80" Text="{Binding NewYear}" PreviewTextInput="YearTextBox_PreviewTextInput"/>
            </StackPanel>

            <Button Content="Dodaj książkę" Click="AddBook_Click" VerticalAlignment="Bottom"/>
        </StackPanel>

        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Books}"
                  SelectedItem="{Binding SelectedBook}"
                  AutoGenerateColumns="False"
                  Margin="0,0,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Tytuł" Binding="{Binding Title}" Width="*"/>
                <DataGridTextColumn Header="Autor" Binding="{Binding Author}" Width="*"/>
                <DataGridTextColumn Header="Rok" Binding="{Binding Year}" Width="100"/>
                <DataGridCheckBoxColumn Header="Przeczytana" Binding="{Binding IsRead, Mode=TwoWay}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,10,0,0">
            <Button Content="Usuń" Margin="0,0,10,0" Click="DeleteBook_Click"/>
            <Button Content="Oznacz jako przeczytaną" Click="MarkAsRead_Click"/>
        </StackPanel>
    </Grid>
</Window>